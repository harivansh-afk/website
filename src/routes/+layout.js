// fully static site: prerender everything. navigation stays client-side
// (hover preloads the next page, clicks swap the dom in place)
export const prerender = true;
export const trailingSlash = "always";
